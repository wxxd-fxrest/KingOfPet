import React from 'react';
import styled from 'styled-components';

const CreateRandomScreen = () => {
    return (
        <Container>
            <Title> CreateRandomScreen </Title>
        </Container>
    );
};

const Container = styled.View``;

const Title = styled.Text``;

export default CreateRandomScreen;
