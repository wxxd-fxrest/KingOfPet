import React from 'react';
import styled from 'styled-components';

const CreateAllPostScreen = () => {
    return (
        <Container>
            <Title> CreateAllPostScreen </Title>
        </Container>
    );
};

const Container = styled.View``;

const Title = styled.Text``;

export default CreateAllPostScreen;
