import React from 'react';
import styled from 'styled-components';

const DiaryDetailScreen = () => {
    return (
        <Container>
            <Title> DiaryDetailScreen</Title>
        </Container>
    );
};

const Container = styled.View``;
const Title = styled.Text``;

export default DiaryDetailScreen;
